# Per-teacher configuration — copy to .env (gitignored). NEVER commit real tokens.
#
# Canvas API token for YOUR account:
#   hw.instructure.com → Account → Settings → Approved Integrations → New Access Token
HUB_TOKEN=

# Optional: where the private finals repo lives. Defaults to ../Admin/finals
# (a sibling checkout of the private Admin repo).
# FINALS_DIR=/absolute/path/to/Admin/finals
